Installation
============

This project is published through PyPI, so the preferred installation method is with 'pip'.

Windows
^^^^^^^
.. code-block::

  pip install --user custconsole

MacOS / Linux
^^^^^^^^^^^^^
.. code-block::

  pip install custconsole